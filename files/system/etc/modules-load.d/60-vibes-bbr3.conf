# Autoload the BBR3 TCP congestion control module at boot so the
# tcp_congestion_control=bbr3 sysctl (60-vibes.conf) always has the module
# available even if the kernel sysctl autoload path fails. The leading
# dash makes the load non-fatal if the kernel ships BBR3 built in.
-tcp_bbr3